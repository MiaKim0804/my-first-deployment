import './App.css';
import config from './config/index';

function App() {
  return (
    
      <header className="App-header">
        <div>BaseURL : {config.baseURL}</div>
      </header>
  
  );
}

export default App;
